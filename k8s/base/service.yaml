apiVersion: v1
kind: Service
metadata:
  name: enterprise-app-service
  namespace: enterprise
spec:
  type: LoadBalancer
  selector:
    app: enterprise-app
  ports:
  - port: 80
    targetPort: 5000
